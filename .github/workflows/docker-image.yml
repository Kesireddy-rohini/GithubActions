name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code on this fresh ubuntu OS
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Set up a unique tag for our docker image
      id: tag
      run: echo "tag=$(date +%s)" >> $GITHUB_ENV

    - name: Build the Docker image
<<<<<<< HEAD
      run: docker build . --file Dockerfile --tag ${{secrets.DOCKER_USERNAME}}/my-python-app:${{ env.tag }}

    - name: Push the Docker image to our DockerHub repo Simple Docker Hub
      run: docker push ${{ secrets.DOCKER_USERNAME }}/my-python-app:${{ env.tag }}
=======
      run: docker build . --file Dockerfile --tag ${{secrets.DOCKER_USERNAME}}/my-python-app:v1

    - name: Push the Docker image to our DockerHub repo Simple Docker Hub
      run: docker push ${{ secrets.DOCKER_USERNAME }}/my-python-app:v1
>>>>>>> 74c9b07a87a36e59aa5cbbbece5be330ac59eae0
